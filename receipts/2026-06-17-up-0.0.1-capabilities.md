# Up 0.0.1 capability production receipt — 2026-06-17

## Environment

- Control/docs: `https://up.ax.cloudflare.dev`
- Dynamic fixture: `https://dynamic-e2e.up.ax.cloudflare.dev`
- Worker Loader, private R2, registry DO, site database DO, site secrets DO, and minute scheduler deployed.
- `workers.dev` and Preview URLs disabled.
- `/app` and `/api` remain protected by Cloudflare Access.
- Wildcard site Access was removed only after the Access-backed Up session broker was verified.

## Company session

An authenticated MyIdentity visit to `/app/__session` minted an HttpOnly, Secure, SameSite=Lax `up_session` scoped to `.up.ax.cloudflare.dev`.

- Authenticated company site returned `200` and identity `jcoeyman@cloudflare.com`.
- Cookie-free company request returned `302` to `/app/__session`.
- The broker validates the return hostname and does not accept external redirects.

## Dynamic runtime

Published `examples/dynamic-site` through the Up UI with company visibility and SQLite enabled.

`GET /api/receipt` returned:

- `runtime: dynamic-worker`
- `outbound: blocked-by-default`
- persistent SQLite receipt rows

The active deployment contained `_worker.js`; static paths continued to use private R2. The Dynamic Worker received only the enabled site database and site secret capability stubs.

## Visibility

Verified transitions for the same active deployment:

1. **company** — signed company session `200`; anonymous request redirected to broker.
2. **public** — cookie-free request returned `200` with the dynamic/database response.
3. **restricted** — reader `jcoeyman@cloudflare.com` with signed session returned `200`; anonymous request redirected to broker.

Local Workers runtime tests additionally prove restricted email, domain, and IdP-group matching plus concealed `404` denial for non-readers.

## Database

- Enabled through publish UI.
- Dynamic code created and wrote the site-specific `receipts` table through `UP_DB`.
- Interactive and scheduled invocations persisted independently of deployment code loading.
- Local tests prove owner/admin lifecycle, forbidden SQL, one-statement limits, and destructive disable.

## Secret capability

Created `CANARY_TOKEN` through Manage with allowlist `api.example.com` and canary value `never-return-production-canary`.

- Management listing returned only name, allowed host, and update time.
- Neither listing nor audit records contained the canary value.
- Dynamic code receives the `UP_SECRETS` capability stub, never the encryption key or plaintext value.
- Local tests prove AES-256-GCM ciphertext, management concealment, host denial, and deletion.

## Schedule

Created `/api/receipt` on `* * * * *` through Manage.

- Scheduler produced repeated SQLite rows with `kind: scheduled`.
- Audit contained `schedule.created`, `schedule.run_succeeded`, and bounded `{ attempt, status }` details.
- Audit contained neither `_worker.js` source nor the canary secret.
- Schedule was changed to `disabled`.
- After a full scheduler interval, the most recent scheduled timestamp remained unchanged.

Local tests additionally prove atomic leasing, retry after failure, success reset, daily quota skip, pause, disable, and deletion.

## Anonymous and public boundaries

- Public docs return `200` without Access.
- `/app` and `/api` return Access redirects anonymously.
- Company/restricted site requests redirect to the protected broker.
- Explicit public site requests return content.
- Private R2 remains inaccessible directly.

## Verification

- Workers runtime tests: 22/22 passed.
- TypeScript: passed.
- Biome: passed.
- Local browser E2E: passed.
- Wrangler dry-run: passed with Worker Loader, three Durable Object classes, and minute cron binding.
- GitHub Actions: green through the latest tested slices.
